#include <iostream>
using namespace std;
long long factorial(int n) {
    return (n == 0) ? 1 : n * factorial(n - 1);
}
int main() {
    int num = 5;
    cout << "Factorial: " << factorial(num);
    return 0;
}
Factorial: 120
